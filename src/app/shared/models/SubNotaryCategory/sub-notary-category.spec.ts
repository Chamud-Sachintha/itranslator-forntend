import { SubNotaryCategory } from './sub-notary-category';

describe('SubNotaryCategory', () => {
  it('should create an instance', () => {
    expect(new SubNotaryCategory()).toBeTruthy();
  });
});
