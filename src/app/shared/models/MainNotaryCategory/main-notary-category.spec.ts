import { MainNotaryCategory } from './main-notary-category';

describe('MainNotaryCategory', () => {
  it('should create an instance', () => {
    expect(new MainNotaryCategory()).toBeTruthy();
  });
});
