import { PassporTranslateModel } from './passpor-translate-model';

describe('PassporTranslateModel', () => {
  it('should create an instance', () => {
    expect(new PassporTranslateModel()).toBeTruthy();
  });
});
